name: Refresh Turso reference data

# Rebuilds the reference data (corpus / dictionaries / grammar / frequency) from
# the upstream aynumosir data repos and re-applies it to the live Turso (libSQL)
# database in place. The glossary is NOT touched here — the MCP reads it live
# from Google Sheets, so it is always current without a scheduler.
#
# Strategy: build the full seed first, VALIDATE it, and only then clear + reload
# the live DB in place (same DB, no worker redeploy). Loading goes through the
# batched libSQL loader (worker/scripts/load-turso.mjs); see docs/REFRESHING-DATA.md.

on:
  schedule:
    # 18:30 UTC on the 1st of each month (~03:30 JST). Reference data from the
    # academic source repos changes rarely, so monthly is plenty.
    - cron: "30 18 1 * *"
  workflow_dispatch:
    inputs:
      reason:
        description: "Why are you running the refresh manually?"
        required: false
  # Let an upstream data repo trigger a refresh on push (optional — see docs).
  repository_dispatch:
    types: [upstream-data-changed]

# Never let two reseeds clobber the same DB concurrently.
concurrency:
  group: refresh-turso
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      AINU_ROOT: ${{ github.workspace }}/_data
    steps:
      - name: Checkout ainu-mcp
        uses: actions/checkout@v7

      - name: Clone private data repos
        env:
          GH_TOKEN: ${{ secrets.DATA_REPOS_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p "$AINU_ROOT"
          # ainu-stopwords is public and tiny — the ETL fetches it straight from
          # GitHub (see ainu_mcp.stopwords), so it is NOT cloned here. Only the
          # ainu-grammar-hokkaido / aynu-itah authored grammar plain text is
          # vendored as a compact snapshot in src/ainu_mcp/data, so this refresh
          # does not need cross-repo access to those source sites.
          repos=(
            "aynumosir/ainu-corpora:ainu-corpora"
            "aynumosir/ainu-dictionaries:ainu-dictionaries"
            "aynumosir/ainu-grammar:ainu-grammar"
          )
          for spec in "${repos[@]}"; do
            repo="${spec%%:*}"
            dir="${spec##*:}"
            echo "::group::clone $repo → $dir"
            git clone --depth 1 \
              "https://x-access-token:${GH_TOKEN}@github.com/$repo.git" \
              "$AINU_ROOT/$dir"
            echo "::endgroup::"
          done

      - name: Set up Rust (corpus builder)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo + builder target
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ${{ env.AINU_ROOT }}/ainu-corpora/builder/target
          key: corpora-builder-${{ runner.os }}-v1

      - name: Build corpus data.jsonl
        working-directory: ${{ env.AINU_ROOT }}/ainu-corpora/builder
        run: cargo run --release -- --format jsonl ../texts > ../data.jsonl

      - name: Set up uv (Python ETL)
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          # Pinned here instead of a root .python-version file: that file makes
          # Cloudflare Workers Builds detect + install Python on every Worker
          # deploy (the Worker is JS-only and lives in worker/). See worker build logs.
          python-version: "3.13"

      - name: Install Python deps
        run: uv sync --frozen

      - name: Build seed
        # GITHUB_TOKEN raises the GitHub API rate limit for the localization
        # gather (src/ainu_mcp/localizations.py fetches public message catalogues
        # straight from GitHub, like the stopwords list).
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: uv run python etl/build_d1.py

      # ───── Safety gate: validate the freshly built data BEFORE touching prod.
      # If the corpus build or ETL produced something implausibly small, abort
      # here — the live DB is still fully intact at this point.
      - name: Validate built seed
        run: |
          set -euo pipefail
          corpus_bytes=$(stat -c%s "$AINU_ROOT/ainu-corpora/data.jsonl")
          echo "corpus data.jsonl: $corpus_bytes bytes"
          if [ "$corpus_bytes" -lt 50000000 ]; then
            echo "::error::corpus data.jsonl is only $corpus_bytes bytes (<50MB) — aborting before touching prod"
            exit 1
          fi
          test -s worker/seed/MANIFEST.txt
          # Expect the big writers to be present.
          ls worker/seed/data/corpus_0001.sql >/dev/null
          ls worker/seed/data/dict_entries_0001.sql >/dev/null
          ls worker/seed/data/dict_fts_0001.sql >/dev/null
          ls worker/seed/data/token_freq_0001.sql >/dev/null
          # Non-empty: stopwords are fetched from GitHub at build time, so a
          # failed fetch must abort here (before prod is touched) rather than
          # silently reseed an empty stopwords table.
          test -s worker/seed/data/stopwords.sql
          # Localization seed: the projects file (one row per curated repo) and
          # the first strings chunk are always written when build_localizations()
          # runs — their absence means the gather step crashed. (Gather is
          # best-effort per repo, so an individual unreachable repo is tolerated;
          # these only guard against the whole step not producing its outputs.)
          test -s worker/seed/data/l10n_projects.sql
          test -s worker/seed/data/l10n_strings_0001.sql
          seed_bytes=$(du -sb worker/seed/data | cut -f1)
          echo "seed/data total: $seed_bytes bytes"
          if [ "$seed_bytes" -lt 20000000 ]; then
            echo "::error::seed/data is only $seed_bytes bytes (<20MB) — aborting before touching prod"
            exit 1
          fi

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install worker deps (@libsql/client for the loader)
        working-directory: worker
        run: bun install --frozen-lockfile

      - name: Apply grammar public-text schema migration
        working-directory: worker
        env:
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
        # --allow-fts-rebuild: this workflow reloads EVERY grammar_fts row right
        # after (reset.sql + full seed), so a destructive FTS rebuild is safe here.
        run: bun scripts/apply-grammar-public-text-migration.mjs --allow-fts-rebuild

      # ───── Reset + reload the live Turso DB in place via the batched libSQL
      # loader (turso db shell drops the stream on the big seed files). During
      # this window (a few minutes, monthly) reference-data searches may return
      # partial results; the glossary is unaffected (it's live from Sheets). The
      # loader applies reset.sql (clear all tables) then the seed in MANIFEST
      # order, and runs a post-reload sanity check (aborts if counts look wrong).
      - name: Reset + reload Turso (in-place)
        working-directory: worker
        env:
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
        run: |
          set -euo pipefail
          files=$(grep -oE 'seed/data/[A-Za-z0-9_./-]+\.sql' seed/MANIFEST.txt)
          bun scripts/load-turso.mjs seed/reset.sql $files
